fn fib(n) {
    let prevprev;
    let prev = 0;
    let curr = 1;

    while n {
        printf("%d\n", curr);

        prevprev = prev;
        prev = curr;
        curr = prev + prevprev;

        n = n - 1;
    }
}

fn main() {
    fib(10);

    exit(0);
}
