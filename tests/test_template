#[test]
fn {name}() -> Result<()> {{
    match run_test(
        "{path}", 
        include_str!("{path}"),
        {is_failing},
        "{expected}"
    ) {{
        Ok(_) => Ok(()),
        Err(e) => {{
            e.print("{path}").unwrap();
            Err(e)
        }}
    }}
}}
