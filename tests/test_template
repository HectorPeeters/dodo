#[test]
fn {name}() -> Result<()> {{
    match run_test(
        "{path}", 
        include_str!("{path}"),
        "{expected}"
    ) {{
        Ok(_) => Ok(()),
        Err(e) => {{
            e.print().unwrap();
            Err(e)
        }}
    }}
}}
